info:
  name: register
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/auth/register"
  body:
    type: json
    data: |-
      {
          "email":"user@admin.com",
          "password":"12345",
          "useername" : "n2500023",
          "first_name":"user",
          "last_name":"admin"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
