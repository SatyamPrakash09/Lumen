info:
  name: agent_stream
  type: http
  seq: 9

http:
  method: POST
  url: "{{base_url}}/sessions/6f8c7023-a1e4-41bb-bef2-a63addeaeac9/agent/stream"
  body:
    type: json
    data: |-
      {
        "query": "search hacker news for tech job security"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
